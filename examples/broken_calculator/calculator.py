"""A deliberately broken calculator for autonomous repair demos."""


def divide(a: float, b: float) -> float:
    """Return the quotient of two numbers (currently implemented incorrectly)."""
    return a * b
